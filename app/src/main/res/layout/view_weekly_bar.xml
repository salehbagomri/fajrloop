<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:orientation="vertical"
    android:gravity="bottom|center_horizontal"
    android:padding="2dp">

    <!-- الحاوية الخلفية للعمود (بمثابة المسار الخلفي الخافت) -->
    <FrameLayout
        android:layout_width="14dp"
        android:layout_height="130dp"
        android:background="@drawable/bg_status_inactive"
        android:layout_marginBottom="6dp">

        <!-- العمود الملون الحقيقي (يُعدل ارتفاعه برمجياً) -->
        <View
            android:id="@+id/view_bar_color"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_gravity="bottom"
            android:background="@drawable/bg_status_active" />
    </FrameLayout>

    <!-- اسم اليوم -->
    <TextView
        android:id="@+id/text_day_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B0C5"
        android:textSize="10sp"
        android:text="سبت"
        android:gravity="center" />
</LinearLayout>
