# Workspace Rules

## Automatic Git Commit & Push
- After completing and verifying any task or set of code changes, ALWAYS automatically stage all modified/new files (`git add .`), commit with a meaningful semantic commit message (`git commit -m "..."`), and push to remote repository (`git push origin main`) without requiring the user to ask or remind you.
